import { Keplr, OfflineDirectSigner, OfflineAminoSigner, AccountData, AminoSignResponse, StdSignDoc, DirectSignResponse, SignDoc, KeplrSignOptions } from "@keplr-wallet/types";
export declare class CosmJSOfflineSignerOnlyAmino implements OfflineAminoSigner {
    protected readonly chainId: string;
    protected readonly keplr: Keplr;
    protected readonly signOptions?: KeplrSignOptions | undefined;
    constructor(chainId: string, keplr: Keplr, signOptions?: KeplrSignOptions | undefined);
    getAccounts(): Promise<AccountData[]>;
    signAmino(signerAddress: string, signDoc: StdSignDoc): Promise<AminoSignResponse>;
    sign(signerAddress: string, signDoc: StdSignDoc): Promise<AminoSignResponse>;
}
export declare class CosmJSOfflineSigner extends CosmJSOfflineSignerOnlyAmino implements OfflineAminoSigner, OfflineDirectSigner {
    constructor(chainId: string, keplr: Keplr, signOptions?: KeplrSignOptions);
    signDirect(signerAddress: string, signDoc: SignDoc): Promise<DirectSignResponse>;
}
