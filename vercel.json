{
  "buildCommand": "cd apps/mint-demo && pnpm install && pnpm run build",
  "outputDirectory": "apps/mint-demo/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
